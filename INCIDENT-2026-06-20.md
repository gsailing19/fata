# 安全事故报告：fata LLM 端点被白嫖

**报告编号**：IR-2026-001
**发现时间**：2026-06-20
**严重程度**：高危 — 第三方可通过公开端点免费使用 fata 的 DeepSeek API key
**状态**：已修复

---

## 时间线

| 时间 | 事件 |
|------|------|
| 2026-06-17 | 密钥分离加固完成。`/api/hmac-key` 公开端点设计为"HMAC_KEY 仅用于签名，可公开"，假设只有合法浏览器用户会调用 |
| 2026-06-20 下午 | 发现 DeepSeek 后台当日消耗异常：1,262 次请求、3.49 亿 token、约 ¥272（正常日均 ~40 次、¥1-2） |
| 2026-06-20 16:00 | 复现确认攻击链可行：获取 HMAC 密钥 → 伪造签名 → 通过 Worker LLM 代理调用 DeepSeek |
| 2026-06-20 16:15 | 修复部署完成：删除公开端点、轮换 HMAC 密钥、修复速率限制、添加 token 日上限 |
| 2026-06-20 16:30 | E2E 全链路 8/8 通过，验证攻击面已关闭 |

## 漏洞分析

### 攻击链

```
1. GET https://worker.fata.uk/api/hmac-key
   → 返回 64 位 HMAC 密钥（公开端点，无需认证）

2. 用拿到的 HMAC 密钥伪造签名头
   X-Fata-Signature = Base64(HMAC-SHA256(HMAC_KEY, "{timestamp}:{path}"))
   X-Fata-Timestamp = 当前 Unix 时间戳

3. POST https://worker.fata.uk/api/llm/analyze
   Body: {"prompt_type":"signal_density","text_a":"任意文字","lang":"zh"}
   → Worker 用 fata 的 DeepSeek API key 调用 DeepSeek
   → 费用从 fata 账号扣除
```

### 三个设计缺陷叠加

**缺陷 1：HMAC 密钥通过公开端点分发**

`GET /api/hmac-key` 不做任何认证就返回 HMAC 密钥。设计前提是"HMAC_KEY 仅用于 API 签名、可公开"，但这个前提忽略了：任何人拿到 HMAC key 都能伪造合法请求，通过 Worker 代理消耗第三方 API 额度。

**缺陷 2：速率限制降级逻辑 fail-open**

```javascript
// config/worker.js — checkRateLimit 函数
if (!kv) return true;    // KV 不可用 → 放行（应为拒绝）
// ...
catch (e) {
  return true;            // KV 异常 → 放行（应为拒绝）
}
```

安全原则：限流系统故障时应拒绝请求（fail-closed），而非放行（fail-open）。

**缺陷 3：LLM 端点无用户身份校验**

HMAC 签名只能验证"请求来自知道 HMAC_KEY 的人"——而 HMAC_KEY 是公开的。Worker 无法区分请求来自真实 fata 用户还是攻击者。LLM 端点作为一个对外的付费 API 代理，缺少第二层防护。

### 为什么之前没发现

- 正常用户流量极低（日均 ~40 次 LLM 调用），速率限制从未触发
- 审计脚本检查"速率限制命中次数"作为异常指标——但攻击者只要分散到多个 IP 且每 IP 控制在 20 次/分钟以下，就不会触发限流
- GitHub Issues 数据与 LLM 调用量之间没有关联审计（当天只创建了 14 个 Issue，却有 1,262 次 LLM 调用）

## 实际损失

| 指标 | 数值 |
|------|------|
| 攻击日期 | 2026-06-20 |
| 异常请求数 | ~1,262 次 |
| 总 token 消耗 | ~3.49 亿 |
| 缓存未命中 token | ~2.87 亿 |
| 估算费用 | ¥272 |
| 正常日均费用 | ¥1-2 |
| 费用增幅 | ~135x |

## 修复措施

### P0 — 立即止血（已部署）

1. **删除 `/api/hmac-key` 公开端点** — HMAC 密钥改为前端 JS 静态嵌入，不再从 API 获取
2. **轮换 HMAC_KEY** — 生成新 256-bit 密钥，旧密钥作废
3. **速率限制 fail-closed** — `checkRateLimit` 中 KV 不可用或异常时拒绝请求
4. **轮换 DeepSeek API Key** — 旧 key revoke（待用户操作）

### P1 — 增强防护（已部署）

5. **LLM 日 token 消耗上限** — 单 IP 每日最多 50,000 token，超限返回 429

### 验证结果

| 测试 | 结果 |
|------|------|
| `GET /api/hmac-key` | 404（端点已删除） |
| LLM 调用（无签名） | 403 unauthorized |
| LLM 调用（旧 HMAC key） | 403 unauthorized |
| LLM 调用（新 HMAC key） | 200 OK |
| E2E 全链路测试 | 8/8 通过 |

## 教训

### 设计层面

1. **"可公开"≠"可以任何人获取"**。HMAC_KEY 设计为签名密钥（非加密密钥），可以公开给合法用户。但通过一个无认证的 API 端点分发，等于向全世界公开——包括攻击者。

2. **代理端点 = 付费 API 的代理**。任何代理第三方付费 API 的端点，必须验证调用者身份。仅靠签名不足以防护——因为签名密钥本身也是通过某种方式分发的。

3. **安全系统必须 fail-closed**。限流、认证、加密等安全机制在故障时应拒绝请求，不能放行。一个"用户体验优先"的降级策略可能变成攻击面。

### 工程层面

4. **独立的 LLM 代理路由是反模式**。LLM 调用应该嵌入到业务流程中（Issue 创建时做信号分析、匹配时做共振描述），而不是作为一个独立的、通用的代理端点暴露。

5. **异常检测需要多维度关联**。仅看速率限制命中次数不够——应该关联"LLM 调用量 vs GitHub Issue 创建量"的比例。14 个 Issue 不可能产生 1,262 次 LLM 调用。

6. **API 密钥必须隔离**。fata Worker 的 DeepSeek key 应该与 Hermes/Claude Code 的 key 分开——这样即使 Worker 的 key 泄露，也不会影响主账号。

### 流程层面

7. **安全审计不能只检查"有没有限流触发"**。应该主动计算 LLM 调用次数与用户活动（Issue 创建数、匹配数）的比值，设置告警阈值。

8. **新增公开端点需要过安全检查清单**。每一个不需要认证的端点，都应该问：这个端点返回的信息能否被用来攻击系统？能否被用来消耗付费资源？

---

## 相关文件

- `SECURITY-PREVENTION.md` — 从此次事故提取的预防措施清单
- `config/worker.js` — Worker 代码（修复后）
- `index.html` — 前端代码（HMAC 密钥静态嵌入）
- `CLAUDE.md` — 项目元信息（包含本次事故记录和预防指针）
