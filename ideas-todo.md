# fata 想法与待办

> 非紧急的想法、待讨论的需求、未来可能做的优化，记录在这里。不丢，不混在代码里。

---

## 待讨论

### 异性匹配

当前系统无性别字段。用户投递时只收集文字+邮箱+隐私偏好，匹配池无性别信息。

需要讨论的问题：
- 前端投递前是否加性别选择（男/女/不限）
- 默认行为：不限，给一个"仅匹配异性"的选项（避免同性用户无法使用）
- 性别是敏感信息，需与邮箱一样 AES 加密存储
- Worker 端匹配逻辑需增加性别过滤

**状态：待讨论，等更好的方案**

---

## 想法池

- （待补充）

---

## 已决策待做

### 退订机制完善

- **状态**：已完成。`sendMatchEmail` 和 `/api/resend/send` 都会在发送前检查 `UNSUBSCRIBE_KV`。

### max_match_count 清理

- **现状**：`algorithm-config.json` 中 `max_match_count = 5`，但代码中没有任何地方使用。匹配成功 Issue 即关闭，单段文字不可能匹配第二次。
- **建议**：删除 `max_match_count`，保留 `max_pending_per_user = 10`（已在配置中但代码未落地检查）。或者新增"单用户最大活跃匹配数"逻辑。
