# fata 安全预防措施清单

> 提取自一次 LLM 端点安全事故。每条规则对应一个具体的设计缺陷。

---

## 一、公开端点原则

### 规则 1：每个公开端点必须过安全检查清单

在 Worker 中添加任何免 HMAC 的路由前，回答三个问题：

- [ ] 这个端点返回的信息能否被用来构造对其他端点的有效请求？
- [ ] 这个端点是否会直接或间接消耗付费 API 额度？
- [ ] 这个端点是否暴露了任何密钥、token、或可用于签名的材料？

**如果任何一个回答"是"，这个端点就不能公开。** 必须加认证层。

### 规则 2：密钥分发不能通过公开 API

签名密钥（如 HMAC_KEY）、API 端点地址、或其他可用于构造有效请求的材料，不能通过公开 API 端点分发。

**正确做法**：嵌入前端静态代码中。这些值的设计前提就是"对合法用户可见"——不需要也不应该通过 API 动态获取。

**错误做法**：`GET /api/hmac-key` → `{ hmac_key: "..." }`（此次事故的入口）

### 规则 3：禁止通用代理端点

Worker 中不得存在"透传"第三方付费 API 的通用代理路由。LLM、邮件、存储等付费 API 调用应该：

- **嵌入业务流程中**（如：创建 Issue 时顺便调 LLM 做信号分析）
- **不能独立调用**（外部无法直接构造 LLM 请求）

如果业务流程确实需要前端触发 LLM 调用（如信号密度实时反馈），则端点必须验证调用者身份——不能仅靠 HMAC 签名。

---

## 二、速率限制规范

### 规则 4：限流系统必须 fail-closed

安全机制故障时，拒绝请求（fail-closed）是唯一正确的选择。用户体验可以通过异步重试优化，但不能以放行作为降级策略。

### 规则 5：多层级限流

仅靠"每分钟 N 次"不够。需要叠加：

| 层级 | 粒度 | 目的 |
|------|------|------|
| 频率限制 | 每分钟/IP | 防突发 |
| 容量限制 | 每日/IP | 防持续消耗 |
| 关联限制 | LLM调用/Issue创建 比值 | 检测异常模式 |

---

## 三、API Key 管理

### 规则 6：付费 API Key 按服务隔离

不同服务使用不同的 API key，不能共享：

- fata Worker → 独立的 DeepSeek API key（`fata-deepseek`）
- Hermes / Claude Code → 独立的 DeepSeek API key（`cc-switch-deepseek`）
- 其他项目 → 各自独立

**理由**：一个服务的 key 泄漏不应影响其他服务。隔离后可以独立 revoke、独立设置额度上限、独立审计。

### 规则 7：付费 API Key 设置用量告警

在 DeepSeek / Resend 等第三方平台设置日用量告警：
- 日费用 > ¥10 → 通知
- 日费用 > ¥50 → 紧急通知 + 自动暂停可疑端点

---

## 四、审计增强

### 规则 8：多维度异常检测

审计脚本不能只检查"速率限制触发次数"。需要加入：

- **LLM 调用次数 vs Issue 创建次数**的比值（正常 ~3-4:1，每个用户创建 Issue 时调 2-3 次 LLM）
- **LLM 日 token 消耗趋势**（与 7 日均值比较，偏离 >300% 触发告警）
- **单 IP LLM 调用集中度**（单一 IP 占总调用 >50% 触发告警）

### 规则 9：速率限制统计包含 LLM 日 token 消耗

在 `getAuditStats` 中添加 `llm-tokens:*` 前缀的 KV 读取，日报中展示每个 IP 的 token 消耗排行。

---

## 五、代码审查清单

任何涉及 Worker API 路由的 PR，审查者必须确认：

- [ ] 新增的公开路由（免 HMAC）是否过了规则 1 的三问检查？
- [ ] 新增的代理端点是否会导致付费 API 被外部调用？（规则 3）
- [ ] 限流逻辑是否 fail-closed？（规则 4）
- [ ] 新增的 API key 是否按服务隔离？（规则 6）
- [ ] 异常检测是否覆盖了新端点？（规则 8）
